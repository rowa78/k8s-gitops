apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: k10-basic-auth
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault-backend
  target:
    name: k10-basic-auth
  data:
    - secretKey: K10_AUTH
      remoteRef:
        key: secret/kasten
        property: auth
    