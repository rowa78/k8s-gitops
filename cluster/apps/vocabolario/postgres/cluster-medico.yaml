apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: medicodb
  annotations:
    k8up.io/backupcommand: sh -c 'pg_dump --clean medicodb'
    k8up.io/file-extension: .sql
    k8up.io/backup: 'false'
    tailscale.com/expose: "true"
spec:
  imageName: 'ghcr.io/cloudnative-pg/postgresql:17.5-202507280807-standard-bullseye'
  instances: 1

  resources:
    requests:
      memory: "500m"
      cpu: "100m"
    limits:
      memory: "6Gi"
      cpu: "4"
  bootstrap:
    initdb:
      database: medicodb
      owner: medico
  # bootstrap:
  #   recovery:
  #     source: vocabolariodb

  postgresql:
    parameters:        
      pg_stat_statements.max: "10000"
      pg_stat_statements.track: all
      hypopg.use_real_oids: 'true'
     
  replicationSlots:
    highAvailability:
      enabled: true

  monitoring:
    enablePodMonitor: true

  enableSuperuserAccess: true

  storage:
    size: 20Gi
    storageClass: openebs-zfs-postgres
  walStorage:
    size: 5Gi
    storageClass: openebs-zfs-postgres

  plugins:
  - name: barman-cloud.cloudnative-pg.io
    isWALArchiver: true
    parameters:
      barmanObjectName: minio
---
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: medicodb-backup
spec:
  schedule: "0 2 0 * * *"
  backupOwnerReference: self
  cluster:
    name: medicodb
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io