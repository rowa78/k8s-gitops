apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: blockydb-secret
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault-backend
  target:
    template:
      data:
        aws_access_key_id: "{{ .BLOCKY_DB_USERNAME }}"
        aws_secret_access_key: "{{ .BLOCKY_DB_PASSWORD }}"
  data:
    - secretKey: BLOCKY_DB_USERNAME
      remoteRef:
        key: secret/blocky
        property: BLOCKY_DB_USERNAME
    - secretKey: BLOCKY_DB_PASSWORD
      remoteRef:
        key: secret/blocky
        property: BLOCKY_DB_USERNAME    
        
