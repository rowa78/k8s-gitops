immich:
  ## This chart relies on the common library chart from bjw-s
  ## You can find it at https://github.com/bjw-s-labs/helm-charts/tree/common-5.0.1/charts/library/common
  ## Refer there for more detail about the supported values

  # These entries are shared between all the Immich components

  controllers:
    main:
      containers:
        main:
          image:
            tag: v3.0.0
          env:
            REDIS_HOSTNAME: '{{ printf "%s-valkey" .Release.Name }}'
            IMMICH_MACHINE_LEARNING_URL: '{{ printf "http://%s-machine-learning:3003" .Release.Name }}'
            # Add the env vars to connect to your database here. see the example below
            #
            DB_HOSTNAME: postgresql.fritz.box
            DB_USERNAME: clash
            DB_DATABASE_NAME: clashdb
            DB_PASSWORD:
              valueFrom:
                secretKeyRef:
                  name: db-secret
                  key: password

  immich:
    metrics:
      # Enabling this will create the service monitors needed to monitor immich with the prometheus operator
      enabled: false
    persistence:
      # Main data store for all photos shared between different components.
      library:
        # Automatically creating the library volume is not supported by this chart
        # You have to specify an existing PVC to use
        existingClaim: immich
    # configuration is immich-config.json converted to yaml. Ignored if existingConfiguration is set.
    # ref: https://immich.app/docs/install/config-file/
    #
    configuration: {}
      # trash:
      #   enabled: false
      #   days: 30
      # storageTemplate:
      #   enabled: true
      #   template: "{{y}}/{{y}}-{{MM}}-{{dd}}/{{filename}}"
    # Sets the resource Kind to store configuration in. Must be either ConfigMap or Secret.
    configurationKind: ConfigMap

    # Specify existing configuration resource name here to skip creation of a new one
    # existingConfiguration:

  # Dependencies

  valkey:
    enabled: true
    controllers:
      main:
        containers:
          main:
            image:
              repository: docker.io/valkey/valkey
              tag: 9.1-alpine@sha256:c9b77919daeba2c02ad954d0c844cc4e7142069d177b89c5fd771f405daf9e02
              pullPolicy: IfNotPresent
    persistence:
      data:
        enabled: true
        type: persistentVolumeClaim
        size: 1Gi
        accessMode: ReadWriteOnce
        storageClass: openebs-hostpath
        # Optional: to keep job queues persistent, replace `type: emptyDir` above with:
        #   type: persistentVolumeClaim
        #   size: 1Gi
        #   accessMode: ReadWriteOnce
        #   # storageClass: your-class

  # Immich components

  server:
    enabled: true
    controllers:
      main:
        containers:
          main:
            image:
              repository: ghcr.io/immich-app/immich-server
              pullPolicy: IfNotPresent
    ingress:
      main:
        enabled: true
        annotations:
          cert-manager.io/cluster-issuer: letsencrypt-production
          # proxy-body-size is set to 0 to remove the body limit on file uploads
          nginx.ingress.kubernetes.io/proxy-body-size: "0"
        hosts:
          - host: clash.rwcloud.org
            paths:
              - path: "/"
                service:
                  identifier: main
        tls: 
          - secretName: start-tls
            hosts:
              - clash.rwcloud.org

  machine-learning:
    enabled: true
    controllers:
      main:
        containers:
          main:
            image:
              repository: ghcr.io/immich-app/immich-machine-learning
              pullPolicy: IfNotPresent
            env:
              TRANSFORMERS_CACHE: /cache
              HF_XET_CACHE: /cache/huggingface-xet
              MPLCONFIGDIR: /cache/matplotlib-config
    persistence:
      cache:
        enabled: true
        type: persistentVolumeClaim
        size: 10Gi
        accessMode: ReadWriteMany
        storageClass: openebs-hostpath
        # Optional: to avoid downloading the ML models on every start, replace `type: emptyDir` above with:
        #   type: persistentVolumeClaim
        #   size: 10Gi
        #   accessMode: ReadWriteMany
        #   # storageClass: your-class

