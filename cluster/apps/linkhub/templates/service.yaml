apiVersion: v1
kind: Service
metadata:
  name: {{ include "linkhub.fullname" . }}
  labels:
    {{- include "linkhub.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "linkhub.selectorLabels" . | nindent 4 }}
