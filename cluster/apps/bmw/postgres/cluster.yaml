apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: bmwdb
  annotations:
    k8up.io/backupcommand: sh -c 'pg_dump --clean bmwdb'
    k8up.io/file-extension: .sql
    k8up.io/backup: 'false'
    tailscale.com/expose: "true"
spec:
  imageName: 'ghcr.io/cloudnative-pg/postgresql:17.7'
  instances: 2

  resources:
    requests:
      memory: "500m"
      cpu: "100m"
    limits:
      memory: "6Gi"
      cpu: "4"
  bootstrap:
    initdb:
      database: bmwdb
      owner: bmw 
  # bootstrap:
  #   recovery:
  #     source: bmwdb

  postgresql:
    parameters:        
      pg_stat_statements.max: "10000"
      pg_stat_statements.track: all
      hypopg.use_real_oids: 'true'
     
  replicationSlots:
    highAvailability:
      enabled: true

  monitoring:
    enablePodMonitor: true

  enableSuperuserAccess: true

  storage:
    size: 20Gi
    storageClass: longhorn
  walStorage:
    size: 5Gi
    storageClass: longhorn

  plugins:
  - name: barman-cloud.cloudnative-pg.io
    isWALArchiver: true
    parameters:
      barmanObjectName: minio
---
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: bmwdb-backup
spec:
  schedule: "0 0 0 * * *"
  backupOwnerReference: self
  cluster:
    name: bmwdb
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io