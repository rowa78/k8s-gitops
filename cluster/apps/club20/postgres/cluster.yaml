apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: club20db
  annotations:
    k8up.io/backupcommand: sh -c 'pg_dump --clean club20db'
    k8up.io/file-extension: .sql
    k8up.io/backup: 'false'
    tailscale.com/expose: "true"
spec:
  imageName: 'ghcr.io/cloudnative-pg/postgresql:17.5-202507280807-standard-bullseye'
  instances: 1

  resources:
    requests:
      memory: "500Mi"
      cpu: "100m"
    limits:
      memory: "2Gi"
      cpu: "4"
  bootstrap:
    initdb:
      database: club20db
      owner: club20 
  # bootstrap:
  #   recovery:
  #     source: club20db

  postgresql:
    parameters:        
      pg_stat_statements.max: "10000"
      pg_stat_statements.track: all
      hypopg.use_real_oids: 'true'
     
  replicationSlots:
    highAvailability:
      enabled: true

  monitoring:
    enablePodMonitor: true

  enableSuperuserAccess: true

  storage:
    size: 10Gi
    storageClass: openebs-hostpath
  walStorage:
    size: 5Gi
    storageClass: openebs-hostpath

  plugins:
  - name: barman-cloud.cloudnative-pg.io
    isWALArchiver: true
    parameters:
      barmanObjectName: minio
---
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: club20db-backup
spec:
  schedule: "0 0 0 * * *"
  backupOwnerReference: self
  cluster:
    name: club20db
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io