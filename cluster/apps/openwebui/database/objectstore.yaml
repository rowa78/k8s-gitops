apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: minio
spec:
  configuration:
    endpointURL: "http://192.168.0.3:9000"      
    destinationPath: "s3://rwcloud-postgres/"       
    s3Credentials:
      accessKeyId:
        name: s3-creds
        key: aws_access_key_id
      secretAccessKey:
        name: s3-creds
        key: aws_secret_access_key
    wal:
      compression: bzip2        
  retentionPolicy: "5d"
