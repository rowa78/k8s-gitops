conninfo host=${DB_HOST} user='${DB_USER}' password='${DB_PASSWORD}' dbname=${DB_NAME}
query_alias SELECT destination FROM virtual_aliases WHERE source=$1;
query_credentials SELECT email, password FROM virtual_users WHERE email=$1;
query_domain SELECT name FROM virtual_domains WHERE name=$1;
query_userinfo SELECT email FROM virtual_users WHERE email=$1;

