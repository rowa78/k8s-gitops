apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: openebs-zfs-default
parameters:
  recordsize: "128k"
  compression: "om"
  dedup: "off"
  fstype: "zfs"
  poolname: "zpool"
provisioner: zfs.csi.openebs.io
volumeBindingMode: WaitForFirstConsumer