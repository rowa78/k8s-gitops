apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: local-path-sdb
provisioner: cluster.local/local-path-provisioner
parameters:
  nodePath: /storage/local
volumeBindingMode: WaitForFirstConsumer
reclaimPolicy: Delete
