ansible_user: ronny
ansible_host: k3s02
ansible_sudo_pass: "{{ lookup('env','TERRA_SUDO_PASS') }}"

k3s_control_node: true

k3s_node_ip: "192.168.0.251,fd00:2222::b8:5fff:fe0a:2467"

# Ubuntu: network_backend defaults to netplan; Debian/terra uses ifupdown explicitly.
# k3s_lan_bridge:
#   name: br0
#   interface: eno1
#   inherit_mac: true
#   dhcp4: true
#   accept_ra: true